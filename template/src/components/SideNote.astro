---
// Locked component. Tufte-style margin note. Authored IN-FLOW right where it
// attaches (can attach to anything) — a superscript marker sits inline in the
// body, the note floats into the right gutter, aligned to its anchor. Numbering
// is automatic (CSS counter); no props to manage.
---
<sup class="hr-sn-ref"></sup><aside class="hr-sn"><slot /></aside>
